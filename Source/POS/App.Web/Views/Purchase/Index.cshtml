@model IEnumerable<App.Web.Mappers.PurchaseDTO>
@*@model PaginatedList<App.Web.Mappers.PurchaseDTO>*@

@{
    ViewData["Title"] = "Index";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.10.21/css/jquery.dataTables.min.css" />
<br />

@if (this.User.IsInRole("Admin"))
{
    <p>
        <a asp-action="Create" class="btn btn-outline-primary">Create New Order</a>
    </p>
}

@*<form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label class="control-label">Customer</label>
            <select class="form-control" asp-items="@(new SelectList(@ViewBag.CustomerList,"Id", "CommercialName"))"></select>
            <a asp-action="Create" asp-route-id="@(new SelectList(@ViewBag.CustomerList,"Id"))" class="btn btn-outline-primary">Add New</a></div>
    </form>*@




<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">POs</h3>
            </div>
            <div class="panel-body">
                <table class="table table-hover table-responsive table-striped" id="ProductsTable">
                    <thead>
                        <tr>
                            <th>
                                PO Id
                            </th>
                            <th>
                                Customer
                            </th>
                            <th>
                                Register
                            </th>
                            <th>
                                Update
                            </th>
                            <th>
                                Items
                            </th>
                            <th>
                                Value
                            </th>
                            <th>
                                Actions
                            </th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Customer.BussinessName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DateUpdate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Orderitemssales.Count)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Value)
                            </td>
                            <td>
                                <a asp-action="AddProduct" asp-route-id="@item.Id" class="btn btn-outline-primary">Add Product</a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-warning">Confirm</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Delete</a>
                            </td>
                        </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="//cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#ProductsTable').DataTable();
        });
    </script>
}